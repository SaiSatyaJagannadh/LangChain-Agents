#my readme file 

UV setup 
uv add langchain langchain-openai langchain-tavily

pip install UV
uv add python-dotenv

ollama - for local 

git status
git add .
git commit -m "environment setup"


github commands to push 

cd ~/path/to/langchain-course
git remote remove origin  # removes the old emarco177 link
git remote add origin https://github.com/SaiSatyaJagannadh/langchain-course.git

git remote -v

git push --set-upstream origin project/React_ToolCall
git commit -m "changes-langsmithtrace" --amend
git push -f

--
git status
git add.
git push --set-upstream origin project/hello-world
git commit -m "changes-langsmithtrace" --amend
git push -f

git fetch origin
git checkout project/ReAct-websearch
---------------

#ollama install for running locally 

ollama pull gemma3:270m 
gpt-oss:20
gpt-oss:20b

ollama list   



#for setting project in pycharm one --ReAct One

 pip3 install pipenv  conda install -c conda-forge pipenv

# Remove the lock file
rm Pipfile.lock

# Install with correct spelling
pipenv install langchain langchain-openai black python-dotenv